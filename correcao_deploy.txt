Resumo solicitado para o deploy do G3

Perguntas pendentes
- O endpoint /actuator/health existe no backend? Se nao existir, devo trocar o healthcheck para outro endpoint.
- O Public Hostname do tunnel no painel da Cloudflare esta apontando para http://frontend:80 e http://backend:8080?

O que ja foi feito
- CORS liberado para https://g3.htasistemas.com.br em backend/src/main/java/br/com/g3/config/CorsConfig.java.
- Criado arquivo de versao: backend/src/main/resources/static/version.txt com 1.00.7.
- Workflow de deploy ajustado para self-hosted e incremento de versao em .github/workflows/deploy.yml.
- Deploy inclui healthcheck simples no container g3-backend (status running).
- docker-compose.yml com nomes fixos de container (g3-db, g3-backend, g3-frontend, g3-tunnel).
- docker-compose.yml com healthchecks e rede dedicada g3_net para o tunnel.
- Dockerfile.backend instala curl para healthcheck do Spring Boot.
- Frontend busca versao disponivel via /version.txt, e backend sincroniza a versao no banco ao detectar divergencia.

O que precisa fazer
- Confirmar se /actuator/health esta habilitado; se nao, ajustar healthcheck no docker-compose.yml.
- Confirmar se o tunnel foi configurado no painel da Cloudflare com os nomes dos servicos internos (frontend/backend) e portas corretas.
- Revisar se o deploy.yml precisa de rollback automatizado ou healthcheck mais completo (ex.: chamada HTTP) no runner.
- Validar se o runner do GitHub Actions tem permissao de push para o bump de versao.
